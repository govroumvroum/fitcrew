{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bunx convex deploy --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL --cmd 'bun run build' && bunx convex run migrations:runAll --prod",
  "git": {
    "deploymentEnabled": {
      "**": false,
      "main": true
    }
  }
}
